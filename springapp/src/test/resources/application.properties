spring.application.name=springapp

rutas.imagenes.url=/imagenes
rutas.imagenes.fisica=src/main/resources/static/imagenes

spring.datasource.url=${JDBC_URL_TEST}
spring.datasource.username=${JDBC_USER_TEST}
spring.datasource.password=${JDBC_PASS_TEST}
spring.datasource.driver-class-name=${JDBC_DRIVER_TEST}

# spring.sql.init.mode=always

spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true

spring.jpa.open-in-view=false

spring.data.rest.base-path=/api/v1

logging.level.root=INFO
logging.level.com.ipartek.formacion=TRACE

# logging.file.path=logs
# logging.file.name=logs/app.log
